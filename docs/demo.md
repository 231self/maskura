# Demo

The recording shows the same PII file written through different plugin
pipelines. It is useful for seeing the interaction, but it is not the evidence:
the old recording predates the current credential flow and cannot assert what
was stored.

<script src="https://asciinema.org/a/8BIbdS1f1J3Ax4A0.js" id="asciicast-8BIbdS1f1J3Ax4A0" async data-autoplay="false"></script>

Run the current, assertion-backed version instead:

```bash
just proof
```

See [Run the claims](proofs.md) for each check and its limits.
