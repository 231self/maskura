# Fly.io app config: Maskura Gateway, scale-to-zero.
#
#   fly launch --no-deploy
#   fly secrets set SUPABASE_URL=... SUPABASE_ANON_KEY=... SUPABASE_JWT_SECRET=... \
#     DATABASE_URL=... MASKURA_SERVICE_BUCKETS=... AUTH_DISABLED=false
#   fly deploy

app = "maskura"

[build]
  image = "ghcr.io/231self/maskura/maskura:latest"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"   # suspend when idle (scale-to-zero)
  auto_start_machines = true    # wake on request
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

# All configuration is runtime env/secrets, never baked into the image.
[env]
  AUTH_DISABLED = "false"
  MASKURA_MAX_OBJECT_BYTES = "67108864"
  MASKURA_MAX_PIPELINE_OUTPUT_BYTES = "67108864"
